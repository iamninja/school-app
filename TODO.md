# Modus — Punch List

Last reviewed: 2026-08-14 (post 1-2 month gap re-orientation)

## Bugs (fix first)

- [x] Fix syntax error / missing `classIds` declaration in `app/auth/parent/actions.ts` (~line 230-283). Project currently fails `tsc --noEmit` and would crash on every parent dashboard load. — Fixed 2026-08-14; also unmasked and fixed 12 downstream `ActionResult` narrowing errors across parent/student login/signup forms and diagnostic.ts files that TS couldn't previously reach.
- [x] Add test coverage for the parent flow (login, signup, dashboard) — this bug shipped specifically because parent has no tests while student/teacher do. — Done 2026-08-14: added `tests/parent-login.test.tsx` (9), `tests/parent-signup.test.tsx` (9), `tests/parent-dashboard.test.tsx` (7). Also found and fixed a nested `<button>`-inside-`<button>` bug in `parent-signup-form.tsx` (the diagnose button and its result panel were inside the submit button — invalid HTML, would have caused the diagnose click to also submit the form) while writing tests for that form.
- [x] Investigate flaky timeout in `tests/teacher-dashboard.student-form.test.tsx` ("does not submit without first name"). — Done 2026-08-14: root cause was Vitest's default 5000ms per-test timeout being too thin for tests that type into 6+ form fields via `user.type()` on a large component (observed durations up to 5030ms, right at the boundary). Raised `testTimeout` to 15000ms in `vitest.config.ts`. Verified with 3 repeated runs of the file (16/16 passing each time) plus a full suite run (66/66).
- [x] Run `npm audit fix` for vitest/vite/ws dev-dependency advisories (dev-only blast radius, but the vitest one is critical-rated and the fix is trivial). — Done 2026-08-14: `npm audit` now reports 0 vulnerabilities (vitest 4.0.18→4.1.10, vite 7.0.x→7.3.6, transitively fixing ws too). Only `package-lock.json` changed — no `package.json` range bumps needed. Verified with `tsc --noEmit` (clean) and full test suite (66/66 passing).
- [x] Fix ESLint config scope — `npx eslint .` returns 15,765 problems, almost certainly linting `node_modules`/`.next` instead of just the project source. Needs an `ignores` entry in `eslint.config.mjs`. — Done 2026-08-14: added `{ ignores: [".next/**", "out/**", "build/**", "coverage/**"] }` to `eslint.config.mjs` (ESLint 9 flat config dropped `.eslintignore` support, so this was never wired up after the migration). Dropped from 15,765 problems to 5 real ones, which were also fixed: 3 unescaped-entity errors, one `require()`-style import in `tailwind.config.ts`, one unused test variable. `npx eslint .` is now clean.
- [x] Production build fails: `parent-login-form.tsx` calls `useSearchParams()` without a `<Suspense>` boundary, which blocks prerendering under Next 16 Cache Components (`next build` errors on `/auth/parent-login`). Pre-existing, found 2026-08-14 while sanity-checking the ESLint fix with a real build. `student-login-form.tsx` has the same `useSearchParams()` pattern and is very likely affected too — needs checking. — Done 2026-08-14: wrapped both login forms in `<Suspense>` at the page level (`app/auth/student-login/page.tsx`, `app/auth/parent-login/page.tsx`). That surfaced a bigger issue underneath: `cacheComponents: true` in `next.config.ts` requires every route reading `cookies()` (i.e. every authenticated dashboard route — parent-dashboard, student-dashboard, protected/teacher, protected) to individually opt into Suspense/`"use cache"`/blocking rendering, and none of those pages benefit from static prerendering since they're 100% personalized authenticated content. Removed `cacheComponents: true` from `next.config.ts` instead of annotating each route. `next build` now completes cleanly — 22/22 routes generated, auth/signup pages static (○), dashboards correctly dynamic (ƒ). Verified with `tsc --noEmit`, `eslint .`, and full test suite (66/66), all clean.
- [x] Public diagnostic endpoints leak student/parent PII without authentication (Opus audit, 2026-08-15). `app/auth/student/diagnostic.ts`'s `diagnosticCheckAction()` takes no arguments, uses the service-role client, and returns id/first_name/last_name/email/user_id for up to 10 students — reachable from the public student signup page (`student-signup-form.tsx`), and because it's a Next.js Server Action its ID ships in the public JS bundle regardless of the debug button around it. `app/auth/parent/diagnostic.ts`'s `diagnoseParentAccountAction(email)` is the same shape against `student_parents` (name/email/phone/student_id/user_id) plus the matching auth user's id/email/created_at, reachable from the public parent login/signup pages. Both were built as debugging aids while wiring up auth and never gated or removed. — Fixed 2026-08-15. The student one had a literal `{/* Diagnostic section - remove after debugging */}` comment and returned unrelated random students' data regardless of input — deleted outright (`app/auth/student/diagnostic.ts`, its import/state/button/JSON-dump in `student-signup-form.tsx`). The parent one had genuine self-service UX value (a friendly "🔍 Diagnose Account Issue" tool on both login and signup pages, only ever returning info for the email typed in) — kept the feature but rewrote `diagnoseParentAccountAction` to return only `status`/`message`/`suggestion`, no `parentRecord`/`authUser` fields at all, and trimmed the UI's `<details>`/JSON-dump blocks that displayed them. Verified with `tsc --noEmit`, `eslint .`, and full test suite (121/121, no test referenced the removed fields), all clean.
- [ ] Student/parent signup only verifies the email is on the roster, not that the requester owns the mailbox (Opus audit, 2026-08-15). `signUpStudentAction`/`signUpParentAction` call `admin.createUser({ email_confirm: true })` with no verification email ever sent — anyone who knows or guesses a roster email can claim that account. Deferred 2026-08-15: the diagnostic-endpoint leak that made this trivially exploitable (queries that map back which roster emails are still "claimable") is now fixed above, and the app isn't live yet, so this moved to Nice-to-haves rather than staying a pre-launch blocker. Needs either a real email-verification step or closing the account-claiming risk some other way — deliberately not designed yet, it's a real UX/feature decision (confirmation-link email vs. OTP vs. something else), not a quick fix.
- [x] No teacher-role check on `/protected/teacher`, only "is logged in" (Opus audit, 2026-08-15). Any authenticated student/parent can currently open the teacher dashboard and create their own classes/students/quizzes under their own account. RLS still correctly prevents them from reading the real teacher's data (independently verified by the audit trying to construct a cross-role leak), so this is a data-integrity/abuse risk rather than a breach — but still needs an explicit role check, not just an auth check. — Fixed 2026-08-15, scoped to an app-layer gate (RLS already holds per the audit's own finding, so hardening every "Teachers manage X" policy too was deliberately deferred as unnecessary risk for a pre-launch app — see the Nice-to-haves note below). New `supabase/teachers.sql` adds a minimal `teachers(user_id)` table with a self-check-only RLS policy (nobody, including the teacher, can read the full roster - not needed for anything today) and new `requireTeacher()` helper (`lib/auth/require-teacher.ts`) called right after the existing `if (!user) throw` check in `/protected/teacher/page.tsx` (redirects to `/` instead of throwing, matching that file's existing style) and all 14 exported functions across `app/protected/teacher/actions.ts`/`quiz-actions.ts`. Requires a one-time manual step after applying the SQL — insert the teacher's own account by email (commented at the bottom of the file) — or the teacher locks themselves out too. While investigating this, noticed `app/protected/students/new/page.tsx` + `student-create-form.tsx` render a form with hardcoded fake class options, disconnected from any real server action — looks like more orphaned scaffold from an earlier session, flagged but not touched (out of scope here, real cleanup candidate later). Verified with `tsc --noEmit`, `eslint .`, full test suite (121/121, unchanged — no test exercises these actions' internals directly), and a production build (19 routes), all clean. Not yet applied against the live Supabase project.

## Missing core features

- [x] Pin the floating `"latest"` dependencies (`next`, `@supabase/ssr`, `@supabase/supabase-js`) to explicit versions; align `eslint-config-next` to actual Next major (16.x). Do this before adding more code. — Done 2026-08-15: pinned `next` to `^16.3.1` (what was actually installed — it had already drifted from 16.1.6 to 16.3.1 mid-session via the floating tag), `@supabase/ssr` to `^0.8.0` and `@supabase/supabase-js` to `^2.95.3` (currently-tested versions, not jumped to newer minors without separately evaluating that). Bumped `eslint-config-next` 15.3.1 → `^16.3.1`, which broke `eslint.config.mjs`: 16.x dropped the old `FlatCompat`-based `next/core-web-vitals`/`next/typescript` string extends in favor of native flat-config exports (`eslint-config-next/core-web-vitals`, `eslint-config-next/typescript`), causing a circular-JSON crash under the old setup. Rewrote the config to import those directly and dropped the now-unused `@eslint/eslintrc` dependency. That upgrade also enabled a new `react-hooks/set-state-in-effect` rule which flagged the standard next-themes hydration-safe-mount pattern in `theme-switcher.tsx` (a legitimate exception, not a real bug) — suppressed narrowly with an inline comment explaining why. Verified with `tsc --noEmit`, `eslint .`, full test suite (66/66), and a production build (22/22 routes), all clean.
- [x] Restore real database-level access control for parent data (RLS via a non-recursive helper function) instead of relying solely on app-layer `user_id` checks. — Done 2026-08-15: new `supabase/parent-rls.sql` adds two `SECURITY DEFINER` helper functions (`is_parent_of_student`, `is_parent_of_class`) and six SELECT-only RLS policies covering `student_parents`, `students`, `student_class_assignments`, `classes`, `class_schedule_slots`, `attendance_records`. The helper-function approach avoids the infinite recursion `parent-auth.sql` originally hit (its comment now points here). Applied by the user against the live Supabase project and verified directly with a role-simulated SQL editor session (`set local role authenticated` + `request.jwt.claim.sub`): a test parent's queries correctly returned only their own child's data across all six tables — including 6 schedule-slot rows that initially looked like a leak but checked out (2 classes × 3 sessions/week). App-layer service-role checks in `app/auth/parent/actions.ts` unchanged — RLS is a second, independent layer underneath them.
- [x] Give teachers a dedicated, branded auth flow instead of the generic starter `/auth/login` — currently the only role without one. — Done 2026-08-15, scoped down after discussion: user is the only teacher for the foreseeable future, so a self-service signup flow (mirroring student/parent's email-verification pattern) isn't worth building — there's no invite list to check against. Bigger issue found along the way: `/auth/sign-up` was completely open to the public with no teacher-role gate anywhere (`/protected/teacher` only checked "is logged in," and every teacher action scoped data by `teacher_id = auth.uid()` with nothing checking who's allowed to hold that role) — anyone who found the URL got a fully working teacher account. Removed public sign-up entirely (`app/auth/login`, `app/auth/sign-up`, `app/auth/sign-up-success`, `components/login-form.tsx`, `components/sign-up-form.tsx`) rather than gating it; the one teacher account now gets created once via the Supabase dashboard, not through the app. Added branded `app/auth/teacher-login` + `components/teacher-login-form.tsx` with a "Forgot your password?" link into the existing (already-generic, already-working) `/auth/forgot-password` → `/auth/update-password` flow — no new password-reset code needed. Fixed every dangling reference to the removed routes: `/protected/teacher` and `/protected` now redirect to `/auth/teacher-login`; `update-password-form.tsx` now lands on `/protected/teacher` after reset instead of the generic `/protected` scaffold page; `auth-button.tsx`'s sign-in link and `logout-button.tsx`'s post-logout redirect updated; the middleware's fallback redirect (`lib/supabase/proxy.ts`) now sends unauthenticated visitors to `/` instead of the deleted `/auth/login` (an improvement for student/parent routes too, which were never correctly served by that generic page anyway). Removed the homepage's now-broken "Next steps" tutorial section and the two orphaned tutorial components it referenced (`connect-supabase-steps.tsx`, `sign-up-user-steps.tsx`) since they linked to the deleted sign-up page. Verified with `tsc --noEmit`, `eslint .`, full test suite (66/66), and a production build (20 routes, down from 22 net of the route changes). A future second-teacher feature would need its own `teachers` table + invite flow, not a revival of open signup.
- [x] Strip remaining scaffold branding (README, nav "Next.js Supabase Starter" text, `shadcn-demo.tsx`, tutorial components) so the app reads as "Modus" everywhere. — Done 2026-08-15. Turned out bigger than cosmetic: `app/protected/layout.tsx` wraps every real authenticated page (including the live teacher dashboard), and it was rendering a "Deploy to Vercel" button and a "Powered by Supabase" marketing link on every page view - not just the homepage. Fixed both `app/page.tsx` and `app/protected/layout.tsx`: nav now says "Modus" (linking home), dropped `DeployButton` and the `EnvVarWarning`/`hasEnvVars` gate on the auth button (that warning's copy also referenced a generic Sign in/Sign up flow that doesn't exist anymore, so it was stale on top of being scaffold), dropped the "Powered by Supabase" footer link. Replaced the homepage's `Hero` (Supabase/Next.js starter-template logos and copy) with a plain "Modus" heading and one-line tagline, and removed `ShadcnDemo` (a kitchen-sink component demo with placeholder copy like "This modal is just a template," and a second, redundant `<Toaster>` instance already mounted globally in `app/layout.tsx`). Deleted `app/protected/page.tsx` too - it was unreachable dead scaffold (a JWT-claims JSON dump + "Next steps" tutorial), confirmed nothing in the app links to bare `/protected`, and the existing middleware already redirects unauthenticated visits there to `/`; an authenticated stray visit now 404s instead of showing raw claims. `app/layout.tsx` metadata (`<title>`/description) rebranded. Deleted the now-fully-orphaned files this left behind (verified via grep before removing): `components/hero.tsx`, `components/shadcn-demo.tsx`, `components/deploy-button.tsx`, `components/env-var-warning.tsx`, `components/next-logo.tsx`, `components/supabase-logo.tsx`, and the whole `components/tutorial/` directory (`fetch-data-steps.tsx`, `tutorial-step.tsx`, `code-block.tsx`). Rewrote `README.md` to describe Modus (stack, local dev setup matching the actual project, role/auth model) instead of the generic starter-kit template copy; added a `"name": "modus"` to `package.json` (had no name field at all before). No test files referenced any of the deleted components (confirmed via grep), so no test changes needed. Verified with `tsc --noEmit`, `eslint .`, full test suite (121/121, unchanged), and a production build (19 routes, down from 20 - the removed bare `/protected` route). Automated in-browser visual check wasn't available in this session (sandbox navigation denied, same as the LaTeX feature); user to spot-check the homepage and an authenticated dashboard page render correctly.
- [x] Decide the multi-teacher/organization data model question before building billing on top of today's `teacher_id`-scoped ownership. — Decided 2026-08-15: deferred, split into two questions. (1) Should billing depend on `teacher_id`? No — decided now, not deferred: an invoice is owed by a family to the business, not to a specific teacher, so myDATA/Viva Wallet tables should reference `student_id` only, never `teacher_id`. This unblocks billing design regardless of the second question. (2) Should multiple teachers ever share visibility into the same student (today's RLS is `teacher_id = auth.uid()` everywhere, fully siloed per teacher — confirmed while reviewing this that a student created by one teacher assigned to a class created by another teacher is invisible to the second teacher entirely, not just "restricted")? Deferred — user is the only teacher for the foreseeable future, so there's no current need to carry that schema/RLS complexity. Revisit alongside whatever "invite a co-teacher" flow gets built if/when a second teacher actually joins; the two are really one design decision (a `teachers`/`organizations` table + shared-roster RLS), not two.
- [x] Password reset flow for student/parent accounts. — Done 2026-08-15: the underlying mechanism (Supabase's `resetPasswordForEmail`/`updateUser` flow) was already role-agnostic and working for teacher, but two real gaps existed. (1) Neither `student-login-form.tsx` nor `parent-login-form.tsx` linked to `/auth/forgot-password` at all — no way to reach it. Added the same "Forgot your password?" link teacher already had to both. (2) `update-password-form.tsx` unconditionally redirected to `/protected/teacher` after reset regardless of who was resetting, since it's one shared component across all three roles. Fixed by branching on `data.user.user_metadata.role` (already stamped as `"student"`/`"parent"` at signup by `signUpStudentAction`/`signUpParentAction`; absent for teacher accounts, which fall through to the teacher dashboard by default) — no new schema or lookups needed. Also changed `forgot-password-form.tsx`'s "Login" link from a hardcoded `/auth/teacher-login` to `/` (home), since the page is now reachable from all three roles and there's no way to know which one without asking. Verified with `tsc --noEmit`, `eslint .`, full test suite (66/66), and a production build (20 routes). Added `tests/update-password.test.tsx` (6 tests) covering the three redirect branches — the genuinely new, silently-regressable logic — plus link assertions in the existing `student-login.test.tsx`/`parent-login.test.tsx`. Suite now 74/74.
- [ ] Design + build myDATA/AADE invoice transmission as a durable background job (queue table with status/retry, not fire-and-forget from the request path).
- [ ] Design + build Viva Wallet billing flow (myDATA fiscalisation API) and Piraeus Bank reconciliation.
- [x] Support more than one child per parent before building billing (Opus audit, 2026-08-15). `student_parents` has a unique index on `email` plus one `student_id` per row, so a single parent email can't span two children today — `createStudentAction` inserts unconditionally, and a second child under an existing parent email throws a raw unique-violation. An invoice is owed by a family (per the earlier `teacher_id`-independent billing decision), so this needs fixing before invoice tables land, not after. — Done 2026-08-15. New `supabase/families.sql` introduces `families` (id, teacher_id, deleted_at) as the billing/household unit; `students` gains `family_id` (not null) and `withdrawn_at` (see the withdrawal item below — built together since both needed the same schema pass); `student_parents` is renamed to `family_parents`, keyed by `family_id` instead of `student_id` — one row per actual parent PERSON per family, not per student-parent pairing, since duplicate per-student rows would have broken `checkParentEmailAction`'s `.single()` email lookup. Backfilled 1:1 (one family per pre-existing student — safe/non-consolidating since the old email-unique constraint already guaranteed no shared parent across students). Rewrote `is_parent_of_student`/`is_parent_of_class` (`parent-rls.sql`) and `is_quiz_assigned_to_parent` (`quizzes-decouple.sql`) to join through the new `family_id` path with unchanged signatures, plus a new `is_parent_of_family` SECURITY DEFINER helper for a `families` RLS policy (a naive subquery there would have reintroduced the exact circular-RLS bug `parent-rls.sql` was written to fix). Teacher UI: an explicit "New family" / "Existing family" radio choice on student creation (not automatic email-matching, per explicit decision) — existing-family path picks from a dropdown and skips parent-contact fields entirely; a duplicate-parent-email insert now surfaces a friendly "use Existing family instead" error rather than a raw Postgres unique-violation. Scope expanded mid-session to include the parent-facing dashboard too (originally going to be data-model-only): `getParentDashboardDataAction`/`ParentDashboardData` restructured from one flat `student` to a `children: ParentDashboardChild[]` array, and `ParentDashboard` now renders a repeated card section per child (chosen over tabs — a parent glancing at the dashboard should see every child's attendance/tuition at once, not discover a hidden tab) instead of one child. Found and fixed two real bugs during live testing after the first pass: (1) the migration's `drop policy ... on public.student_parents` wasn't guarded against a second run once the table had already been renamed — Postgres errors on a nonexistent relation regardless of `if exists` on the policy, which blocked recovery after a partial first run; wrapped in a table-existence check. (2) `families` is embedded in `app/protected/teacher/page.tsx`'s student query via the forward FK on `students.family_id`, so Supabase actually returns it as a single object at runtime — but without a `Database` generic on the client, TypeScript infers every embed as an array, and indexing `[0]` into the real single-object response silently returned `undefined` and dropped every family's parent contacts on every page load (a freshly-created child appeared fine only because it read parent info from the create action's return value, never through this query). Fixed with an explicit type cast matching the real runtime shape instead of changing the code to match the wrong inferred type. Verified with `tsc --noEmit`, `eslint .`, full test suite (126/126, up from 121), and a production build, all clean; applied against the live Supabase project and live-tested by the user (sibling enrollment via both New and Existing family, duplicate-email error, multi-child parent login) after the two bugs above were found and fixed. Committed as `15dc5f5`.
- [ ] Decide the fate of `tuition_amount`/`tuition_status` once real invoicing lands (Opus audit, 2026-08-15) — currently a denormalized stand-in on the student record that billing should probably supersede rather than duplicate alongside.
- [ ] Add `on delete restrict` (not cascade) for future invoice/payment FKs referencing students (Opus audit, 2026-08-15) — student deletes currently cascade, which would silently destroy financial records once billing tables exist.
- [x] Formal migration tracking for `supabase/*.sql` before billing schema work starts (Opus audit, 2026-08-15) — the files were unversioned/hand-applied, fine so far but riskier once changes are feeding AADE-reconstructible records. — Done 2026-08-15: adopted the Supabase CLI's tracked-migration workflow. `npm install --save-dev supabase` (pinned, `2.114.0`), `npx supabase init`, linked to the live project (`daeehagpcboyfhbfoqgu`). Rather than hand-converting the 9 existing schema files into individual migrations (risking transcription drift), used `npx supabase db pull` to generate one authoritative baseline migration (`supabase/migrations/20260815175602_remote_schema.sql`) directly from the live database's actual current schema — verified it contains all 15 tables including `families`/`family_parents`/`withdrawn_at` from the same-day families work. The original hand-applied files moved to `supabase/history/` (superseded, kept for their explanatory comments — e.g. the RLS-recursion story in `parent-rls.sql`); the two diagnostic-only scripts moved to `supabase/diagnostics/`; `seed-teacher-dashboard.sql` renamed to `supabase/seed.sql` (CLI convention, auto-run by `db reset`, never pushed to a real project). New `supabase/README.md` documents the go-forward workflow (`npm run db:migration -- <name>` → edit → `npm run db:push`) and the new-project bootstrap path (`supabase link` to a fresh project + `db:push` replays the full tracked history — this is also what a real, clean-data production database gets stood up from at launch). Root `README.md` updated to point at the new workflow instead of "paste into the SQL editor." Hit two real snags along the way, both resolved: (1) `db pull`/`migration list` fail with a Supabase-platform-side bug (`LegacyDbConfigLoginRoleStatusError`, permission denied altering a temporary `cli_login_postgres` role) when connecting without a password — worked around by passing `--password` directly, which uses a real DB connection instead of the buggy passwordless role-provisioning path; (2) `db pull`/`db dump` both require Docker (bundles a containerized `pg_dump` for version-matching, not optional despite research initially suggesting `db dump` might skip it) — Docker Desktop was already installed on the user's machine but its bin directory wasn't resolvable from any already-running shell (PATH was correctly set at the OS level, just not refreshed into already-running process trees) — worked around by having the user run the actual pull command from a fresh terminal window instead of one of my own tool-session shells. `package.json` scripts gained `db:pull`/`db:push`/`db:diff`/`db:migration`. Verification: full suite (`tsc`/`eslint`/`vitest`/`next build`) unaffected since no app code changed, just tooling/docs; `supabase/migrations/20260815175602_remote_schema.sql` confirmed to contain the expected 15 tables and every table/RLS/function added this session.
- [x] Quizzes/tests feature — teachers create quizzes (multiple choice, true/false, short answer) tied to a class; students take them (single attempt) and get an immediate review; parents see results, mirroring attendance. Not originally on this list — added mid-session 2026-08-15 as a new feature request, planned via `/plan` (see `~/.claude/plans/replicated-sauteeing-map.md`) before building. New `supabase/quizzes.sql` (5 tables + RLS via a new `is_student_of_class` SECURITY DEFINER helper, same recursion-avoidance pattern as `parent-rls.sql`), `app/protected/teacher/quiz-actions.ts`, `app/student-dashboard/quiz-actions.ts`, a new `components/ui/radio-group.tsx` primitive, and three new dashboard UI pieces (`teacher-quiz-builder.tsx`, `student-quiz-panel.tsx`, a Quiz Results card in `parent-dashboard.tsx`). Answer key (`is_correct`) is never sent to the browser before submission — enforced by explicit column selection in the fetch action, not by RLS (which is row-level, not column-level); grading happens server-side against the real answer key, never trusting a client-submitted correctness claim. Short-answer questions can be authored and answered now, but manual grading of them is intentionally out of scope for this pass (`points_awarded` stays null, shown as "awaiting review") — flagged as the natural fast-follow. Multiple attempts and cross-student-visibility for multi-teacher classes are both deferred, matching the same "solo teacher for now" reasoning as other decisions on this list. `supabase/quizzes.sql` applied to the live Supabase project 2026-08-15 and confirmed working end-to-end by the user (created a quiz as teacher, took it, checked results) — behaved as intended. Test coverage audited afterward and two real gaps closed: the parent Quiz Results card had zero coverage, and `true_false`/`short_answer` question types were never exercised anywhere (creation, taking, or review) despite being part of the feature from day one. Added parent card tests, teacher-side true/false and short-answer creation tests, and student-side true/false-taking, short-answer-taking, and short-answer-review tests. Also gave the teacher builder's question-type `<select>` an `aria-label` — it had no accessible name at all before this. Verified with `tsc --noEmit`, `eslint .`, full test suite (95/95, up from 74), and a production build (20 routes), all clean.
- [x] Quiz list doesn't update after a student submits (still shows "Take Quiz" until a full page reload) and the review view was rendering blank where the question text should be. — Fixed 2026-08-15, found via live testing right after the feature shipped. Two separate bugs: (1) `StudentQuizPanel` received `quizzes` as a prop and never updated it locally after a successful submission — lifted it into local state, updated in place on submit. (2) `getQuizReviewAction` (and, found by inspection while fixing it, the same pattern in the teacher's `getQuizResultsAction`) embedded a related table via PostgREST's `alias:fk_column(...)` join syntax and cast the result assuming a single object; the embed's actual shape wasn't reliable, silently returning `undefined` for the nested fields it fed into. Both rewritten to fetch the related rows in a separate query and join them in JS by ID — the same approach `getQuizForTakingAction`/`submitQuizAttemptAction` already used successfully, so this converges the whole feature on one proven pattern instead of two. A third bug turned up right after: the question text in the review *was* rendering, just nearly invisible in dark mode — the correct/incorrect cards used hardcoded light-only backgrounds (`bg-green-50`/`bg-red-50`) with no `dark:` variant, so the ambient near-white dark-mode text sat on a pale pastel background with almost no contrast. Fixed by adding explicit `dark:` background/border/text variants to those two states.
- [x] Teacher had no way to review a specific student's answers on a taken quiz — only saw the aggregate score. Found live, right after the previous fixes. — Fixed 2026-08-15: new `getStudentQuizAttemptAction` (`app/protected/teacher/quiz-actions.ts`) returns the same `QuizAttemptReview` shape as the student's own `getQuizReviewAction`, scoped by teacher ownership of the quiz rather than by "is this the caller's own attempt." Completed rows in the teacher's results view are now a "score · View answers" button leading to the same answer breakdown a student sees. While building this, also noticed the student's own review only ever showed a bare Correct/Incorrect badge — no indication of what they picked or what the right answer was, which makes "review" not very useful. Extended `QuizAttemptAnswerReview` with `selectedOptionText`/`correctOptionText` (populated in all three places that build this type: `submitQuizAttemptAction`, `getQuizReviewAction`, and the new teacher action) and extracted the rendering into a shared `components/quiz-review-answers.tsx` used by both the student review and the new teacher view, instead of duplicating the same JSX twice. Added `tests/teacher-quiz-builder.test.tsx` coverage for the new flow. Verified with `tsc --noEmit`, `eslint .`, full test suite (88/88, up from 87), and a production build (20 routes).
- [x] LaTeX/math rendering support in quiz questions and answers — this is a tutoring center, quizzes will be mostly math. — Done 2026-08-15 on branch `feature-quiz`. Chose KaTeX over MathJax (fast, synchronous, no network round trip; its documented gaps — chemistry notation, TeX conditionals, cross-referencing — don't touch algebra/geometry/trig/calculus). New `components/math-text.tsx` (`MathText`) parses `$...$` inline and `$$...$$` display math out of a string and renders it via `katex.renderToString`, falling through to a plain text node with zero behavior change when there's no `$` present. Wired into question/option text everywhere it's authored or displayed: the shared `quiz-question-editor.tsx` (with a live rendered preview under the input whenever it contains a `$`, so a teacher catches a typo before assigning the quiz — covers both the create and edit dialogs for free since they share the component), `quiz-review-answers.tsx` (student's own review + teacher's per-student view), `student-quiz-panel.tsx` (taking view), and the per-question breakdown view in `teacher-quiz-builder.tsx`. Deliberately never rendered: a student's own free-text short-answer response (`textAnswer`) — `MathText` uses `dangerouslySetInnerHTML` for KaTeX's output, so only teacher-authored content (question/option text) is ever fed into it; several call sites that previously did `selectedOptionText ?? textAnswer ?? "(no answer)"` had to be split into explicit branches rather than wrapped directly, since a naive wrap would have run a student's own text through the math renderer whenever they hadn't picked an option. Extended the same session to quiz title/description: live preview under both fields in the create and edit dialogs, and `MathText` wrapping every place a quiz title renders as text (teacher's quiz list, results header, breakdown header, "Manage classes" dialog title; student's quiz list, taking view, review header; parent's Quiz Results card). Description has no display surface elsewhere in the app yet (it's authored but never shown to students/parents — a pre-existing gap, not something this task added), so its LaTeX support today is the live preview only. Verified with `tsc --noEmit`, `eslint .`, full test suite (116/116, up from 104, including a dedicated `tests/math-text.test.tsx` and regression tests proving student free text is never rendered as math even when it contains a literal `$`), and a production build (20 routes). Automated in-browser visual check wasn't available in this session (sandbox navigation denied); user to spot-check math glyphs render correctly against their already-running dev server.
- [x] Quiz editing, reuse, and reassignment — teacher should be able to edit a saved quiz and re-assign it to the same or a different class rather than only ever creating new ones. — Done 2026-08-15 on branch `feature-quiz`, logic only (UI follow-ups tracked separately). Quizzes were previously hard-bound to exactly one class via `quizzes.class_id NOT NULL`; new `supabase/quizzes-decouple.sql` drops that column and adds a `quiz_assignments` many-to-many join table (with backfill from the old data) plus new `is_quiz_assigned_to_student`/`is_quiz_assigned_to_parent` SECURITY DEFINER RLS helpers routed through it, same non-recursive pattern as `is_student_of_class`/`is_parent_of_class`. A quiz now belongs to a teacher only; class assignment is a separate concern, so one quiz can be reused across multiple classes/terms. `updateQuizAction` lets a teacher edit title/description anytime; the question/option editor locks once the quiz has any student attempt (checked server-side, not just in the UI) to protect past scores/reviews — `duplicateQuizAction` is the escape hatch, copying a quiz's content into a brand-new unassigned, unlocked quiz so a locked one can still be iterated on. Applied against the live Supabase project and live-tested by the user. Verified with `tsc --noEmit`, `eslint .`, full test suite (101/101, up from 95), and a production build (20 routes), all clean. Committed as `6c6c3d9`.
- [x] Teacher should be able to unassign an already-assigned quiz from a class. — Done 2026-08-15 together with the item above (same `quiz_assignments` change) — new `assignQuizToClassAction`/`unassignQuizFromClassAction` insert/delete a single join-table row, exposed in the teacher UI via a "Manage classes" checkbox dialog per quiz.
- [x] Timer support for quizzes (time limit, auto-submit when it runs out). — Done 2026-08-15 on branch `feature-quiz`, barebones by design (no anti-cheat, no pause/resume UI). New `supabase/quiz-timer.sql` adds `quizzes.time_limit_minutes` (nullable, optional) and a small `quiz_attempt_starts` table recording when a student first opened a timed quiz — needed so the countdown survives a page refresh (resumes from the true remaining time) instead of resetting, per explicit decision with the user. `getQuizForTakingAction` upserts a start-time row only for quizzes that actually have a limit; `submitQuizAttemptAction` cleans it up on submit (tidy, not load-bearing — the existing `quiz_attempts` unique constraint already blocks a second attempt). Time limit threaded through the same "always editable, never locked" path as title/description in `createQuizAction`/`updateQuizAction`/`getQuizForEditingAction`/`duplicateQuizAction`/`buildQuizListItem`. Teacher UI: a "Time limit in minutes (optional)" input in both the create and edit quiz dialogs, plus a "⏱ N min" badge in the quiz list when set. Student UI: a live mm:ss countdown badge in the taking view (turns to a destructive/red badge under a minute), and the quiz auto-submits with whatever's currently answered once time runs out — reusing the same submission path as the Submit button via a `{ auto: true }` flag that skips the "answer all questions" validation, with a distinct "Time's up — quiz submitted automatically" toast. Hit one real lint finding while building this: the countdown-reset early-return synchronously called `setState` inside a `useEffect`, tripping `react-hooks/set-state-in-effect` — suppressed narrowly with an inline comment, matching the one other place in this codebase that already does this (`theme-switcher.tsx`'s hydration-safe-mount pattern), since there's no external system to subscribe to here, just derived state that needs to stop showing stale time. Verified with `tsc --noEmit`, `eslint .`, full test suite (121/121, up from 116, including a countdown-display test and an auto-submit-on-elapsed-limit test that avoid fake timers entirely by setting `startedAt` far enough in the past that the deadline has already passed on mount), and a production build (20 routes). Not yet applied against the live Supabase project or manually browser-tested — user to apply `supabase/quiz-timer.sql` and spot-check the countdown/auto-submit/refresh-resume behavior end-to-end.
- [x] Per-question view for the teacher — see every student's answer to a single question across the whole class, not just per-student across all questions (which is what `getStudentQuizAttemptAction` builds today). Useful for spotting a question everyone missed or a common wrong answer. — Done 2026-08-15 on branch `feature-quiz`. New `getQuizQuestionBreakdownAction` (`app/protected/teacher/quiz-actions.ts`) is the per-question complement to the existing per-student `getQuizResultsAction`: for each question it returns an option-by-option answer count (correct option flagged) plus every attempting student's individual answer, sorted alphabetically. Short-answer questions skip the option distribution (there isn't one) and show each student's free-text answer with an "Awaiting review" badge, matching the existing ungraded-short-answer convention. Surfaced in the teacher UI as a new "Question breakdown" button per quiz (kept as its own view rather than folded into the existing per-student results screen, per user preference), reusing the same green-highlight-for-correct visual language as `quiz-review-answers.tsx`. Verified with `tsc --noEmit`, `eslint .`, full test suite (103/103, up from 101), and a production build (20 routes), all clean.

## Nice-to-haves

- [x] Shared auth-action factory to de-duplicate the three near-identical role flows (teacher/student/parent `checkXEmail`/`signUpX`/`signInX`). — Done 2026-08-15. Teacher turned out to have neither `checkXEmail` nor `signUpX` (no signup flow at all — the one account is created manually via the Supabase dashboard) and its sign-in is architecturally different (a client component calling `supabase.auth.signInWithPassword` directly, not a server action), so it stayed out of scope; the real duplication was entirely between student and parent. New `lib/auth/role-account-actions.ts` (`lookupRoleEmail`/`createRoleAuthUser`/`signInAsRole`, parametrized by role/table/column names) with `app/auth/student/actions.ts`/`parent/actions.ts` reduced to thin wrappers around it — exported function names/signatures/return types unchanged, so every existing auth test (`student-login`, `student-signup`, `parent-login`, `parent-signup`) mocks at that same boundary and needed zero edits. Normalized two small internal-only deltas between the roles with the user's explicit sign-off first: dropped student's redundant second `getUser()` call after `signInWithPassword` already returned the user (parent didn't have it), and added parent's role-verification-failure log to student's path too (parent had it, student didn't) — neither changes what a user experiences. `getStudentDashboardDataAction`/`getParentDashboardDataAction` untouched (not part of the duplicated shape). `app/auth/*/diagnostic.ts` flagged as a natural follow-on (same duplication pattern) but not bundled in. Verified with `tsc --noEmit`, `eslint .`, full test suite (121/121, unchanged), and a production build (19 routes), all clean.
- [x] Soft-delete/withdrawal state for student enrollment history. — Done 2026-08-15, built together with the families entity above (audit finding D4 — invoice tables need `on delete restrict`, not cascade, and this soft-delete state becomes load-bearing once a student can have issued invoices) via `supabase/families.sql`. `students.withdrawn_at` (nullable timestamptz) plus `families.deleted_at` (schema only for now — no delete UI/action yet, no clear trigger for it until a families-list view exists, just didn't want to paint the schema into a corner). New `withdrawStudentAction`/`restoreStudentAction` (`app/protected/teacher/actions.ts`). Withdrawn students are hidden by default from the teacher roster and the attendance-taking roster, with a "Show withdrawn" toggle to reveal them (with Restore); the parent dashboard also excludes withdrawn children by default. No hard delete anywhere — matches that nothing in the app calls a delete today. Verified and committed together with the item above (`15dc5f5`).
- [ ] Add real email verification to student/parent signup (Opus audit, 2026-08-15; moved here from Bugs 2026-08-15 since the app isn't live yet). `signUpStudentAction`/`signUpParentAction` currently only verify the email is on the roster, not that the requester owns the mailbox — needs a real design pass (confirmation-link email vs. OTP vs. something else) before building, not a quick fix.
- [ ] Harden RLS policies with an explicit `is_teacher()` check as defense-in-depth, on top of the app-layer `requireTeacher()` gate added 2026-08-15 (see Bugs section) — deliberately deferred at gate-implementation time since RLS already correctly scopes all teacher-owned data by `teacher_id = auth.uid()` (audit-verified no cross-role leak exists), so this is redundant hardening rather than a fix for a real gap. Would touch every "Teachers manage X" policy across `quizzes.sql`/`teacher-dashboard.sql`/etc. — bigger diff, worth doing eventually but not urgent for a pre-launch app.
- [ ] Drop the stale service-role bypass on the parent dashboard read path (Opus audit, 2026-08-15) — `getParentDashboardDataAction` reads via `createServiceRoleClient()` with comments saying "avoids RLS recursion," but that recursion was already fixed by `supabase/parent-rls.sql`; the bypass looks unnecessary on the main path now and won't port to a future mobile app talking directly to Supabase.
- [ ] Add RLS-level integration test coverage (Opus audit, 2026-08-15) — every existing test mocks at the Supabase client boundary, so nothing in the suite actually exercises an RLS policy; today that's only ever verified manually via role-simulated SQL editor sessions (see `supabase/verify-parent-rls.sql`). Worth at least a handful of real integration tests.
- [ ] The teacher CRUD server actions (`app/protected/teacher/actions.ts`) are only ever exercised indirectly through mocked UI-component tests (Opus audit, 2026-08-15) — no test calls the actions' own logic directly, so a bug inside the action itself could slip through untested even with the UI tests green.
- [x] Remove the dead `@eslint/eslintrc` devDependency (Opus audit, 2026-08-15) — the 2026-08-15 `eslint-config-next` 16.x migration entry above already claims this was dropped, but it's still present in `package.json` and unused by `eslint.config.mjs`; the removal apparently didn't stick. — Done 2026-08-15: confirmed genuinely unused (grepped, zero references anywhere in the codebase — `eslint.config.mjs` only imports `eslint-config-next/core-web-vitals` and `eslint-config-next/typescript`), then `npm uninstall @eslint/eslintrc`. Verified with `tsc --noEmit`, `eslint .`, full test suite (126/126, unchanged), and a production build, all clean.
- [x] Upgrade `@supabase/ssr` 0.8.0 → 0.12.4 (Opus audit, 2026-08-15) — the one dependency worth scheduling; no known vulnerability, just meaningfully behind. — Done 2026-08-15. Checked the changelog first: 0.12.0 was a "full rewrite" of the cookie-handling API (`getAll`/`setAll`), but `lib/supabase/server.ts`, `lib/supabase/client.ts`, and `lib/supabase/proxy.ts` were already using that exact pattern (not the older per-cookie `get`/`set`/`remove` API), so the rewrite wasn't a breaking change here. `npm install @supabase/ssr@^0.12.4`. Verified with `tsc --noEmit`, `eslint .`, full test suite (126/126, unchanged), and a production build, all clean — but every test in this suite mocks the Supabase client boundary, so none of that actually exercises real cookie/session handling; user to log in as each role (teacher/student/parent) and confirm sessions persist across a reload before trusting this in production.
- [x] Add error tracking/monitoring (Opus audit, 2026-08-15) — confirmed absent; currently zero visibility into production errors. — Done 2026-08-15. Added `@sentry/nextjs` (`^10.70.0`), set up manually rather than via the interactive `npx @sentry/wizard` (avoided another browser-OAuth round-trip after the day's Supabase CLI login/Docker detour, and it let the PII-scrubbing requirement below be built in from the start). `instrumentation.ts` exports `onRequestError = captureRequestError`, which auto-captures every thrown server action/route handler/server component error — a good fit for this codebase's existing "just `throw error`, don't catch" convention (confirmed zero `catch` blocks in `app/` beforehand), no per-action changes needed for the ~40 existing throw sites. New `app/error.tsx`/`app/global-error.tsx` — the app had zero Next.js error boundaries before this, so an uncaught error previously fell through to Next's raw default page; now reports to Sentry and shows a styled fallback matching the app's existing "Access Denied" full-page pattern. PII: confirmed with user that no identifying info (name/email/phone/user id) should reach Sentry, only a coarse role. `sendDefaultPii: false` plus `lib/sentry-scrub.ts` (denylist-based `beforeSend` scrubber, defense-in-depth for any future `captureException(err, { extra })` call) and no session replay (deliberately never touched — a tutoring app for minors is exactly the case where session recording needs its own decision, not a default-on SDK feature). Also addressed a real follow-up question during design: since every thrown server-action error gets auto-reported, two existing messages that are expected user-facing outcomes (not bugs) needed filtering out or they'd be false-alarm noise on every normal occurrence — `createStudentAction`'s "email already registered to another family, use Existing family instead" and `updateQuizAction`'s "quiz has submissions, can't edit questions" — found via grepping every `throw` in `app/`. New minimal `lib/expected-error.ts` (`ExpectedError extends Error`) marks these two call sites; `beforeSend` skips reporting instances of it. `next.config.ts` wrapped with `withSentryConfig` for build-time source-map upload (no-op without `SENTRY_AUTH_TOKEN`, which — like `SENTRY_ORG`/`SENTRY_PROJECT` — belongs in Vercel's project env vars, not `.env.local`). `.env.example` gained `NEXT_PUBLIC_SENTRY_DSN` (public value, safe to commit as a placeholder). Verified with `tsc --noEmit`, `eslint .`, full test suite (126/126, unchanged), and a production build, all clean. Not yet verified end-to-end against a real Sentry project — this sandbox's auth middleware blocks unauthenticated access to any test route outside `/auth/`, so a live error-boundary/dashboard check needs the user's own browser session; user still needs to create a Sentry account + Next.js project, set `NEXT_PUBLIC_SENTRY_DSN`, and confirm a deliberate test error actually shows up in the dashboard.
- [ ] When building the mobile app, extract the logic that must stay server-side (quiz grading, signup flows) from the thin CRUD server actions that a mobile app could call directly against PostgREST/Supabase instead (Opus audit, 2026-08-15) — not worth restructuring now per the audit's own recommendation, just don't lose this framing when that work starts.
- [x] Investigate Vitest per-run overhead — full suite (66 tests) takes 45-65s (one outlier at 98s), and the `import`/`environment`/`setup` phases regularly outweigh actual test execution time (e.g. one run: 178s environment vs 66s tests). Not urgent at this size, but won't get faster on its own as the suite grows since it's fixed overhead, not per-test cost. Check Vitest `pool`/thread config for unnecessary jsdom re-initialization per file. — Done 2026-08-15. Confirmed the hypothesis: by default each of the 11 test files gets its own fresh jsdom environment and module graph (React/Radix/KaTeX etc. all re-imported from scratch per file), and with 8 CPU-count workers for 11 files, most workers only ever process 1 file anyway - no sharing, all cost. Measured baseline: 121 tests, ~52-54s wall time, ~30-40s cumulative `import` + ~20-30s cumulative `environment` on top of ~100-135s cumulative `tests` (numbers are summed across parallel workers, not wall-clock). Tried in order: `isolate: false` alone (files sharing a worker reuse jsdom/module cache) - modest ~48s, real but small since sharing was still rare; `fileParallelism: false` (single worker, maximum reuse) - **made things worse and broke 17 tests** across 2 files with cross-file state pollution (mocked-module registration conflicts), reverted immediately, not safe for this suite; `pool: "vmThreads"` - no consistent improvement over plain `threads` and carries more native-addon compatibility risk, not worth it. Landed on `isolate: false` + `pool: "threads"` + `maxWorkers: 4` (capped below the 8-CPU default so more files land per worker and there's actually something to reuse) - stable ~42-45s across 6 repeated runs, all 121/121 passing every time, with cumulative `setup`/`import`/`environment` cut roughly in half to two-thirds versus baseline. Config change is entirely in `vitest.config.ts` (with an explanatory comment on the trade-off and the "why 4, not more/fewer" reasoning) - no test files touched, no `next build` impact (confirmed clean). If the suite grows a lot, worth re-measuring `maxWorkers` rather than assuming 4 stays optimal.

Business/financial items (legal business form, launch costs, operational budgeting) are tracked in Todoist only, not here — this file is code-focused and pushed to a public repo.

---
*See full re-orientation assessment from 2026-08-14 conversation for context behind each item.*
